<% drone_id = ENV.fetch('DRONE_ID', 1).to_i %>

{
  plugins: {
    ros2dds: {
      allow: {
        publishers: ["/state_sharing_drone_<%= drone_id %>"],
        subscribers: ["/tracks", "/dtc_commands", "/state_sharing_drone_.*"]
      }
    }
  },
  scouting: {
    multicast: {
      enabled: true
    }
  }
}